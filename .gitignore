.DS_Store
node_modules/
*.swp
Thumbs.db
*.zip
store-assets/_*

# WXT build artifacts
.output/
.wxt/

# Env overrides — `.env.example` + `.env.development` are committed (no
# secrets, just localhost URLs); `.env`, `.env.local`, and `.env.*.local`
# stay per-developer.
.env
.env.local
.env.*.local

# Test artifacts (không commit)
test-results/
playwright-report/

# Understand-Anything knowledge graph (giữ local, không commit)
.understand-anything/
